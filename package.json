{
  "name": "de-stampertjes-test",
  "version": "2.5.0-beta.1",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1"
  },
  "devDependencies": {"@playwright/test": "1.58.2", "jsdom": "26.1.0"}
}
